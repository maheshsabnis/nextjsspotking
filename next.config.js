/** @type {import('next').NextConfig} */

/* RUntime Config
Env  declaration e.g. App Secrets

publicRuntimeConfig: Configure the FOlders
those are available on Client as well as on server
*/
const nextConfig = {
  reactStrictMode: true,
}

module.exports = nextConfig
